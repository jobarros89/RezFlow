import { useRouter } from 'expo-router'; import { StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Button } from '@/components/ui/Button'; import { colors,fontFamily,spacing } from '@/constants/theme'; import { authService } from '@/services/auth.service';
export default function Home(){const r=useRouter();const leave=async()=>{await authService.signOut();r.replace('/auth/login');};return <SafeAreaView style={s.safe}><View style={s.content}><Text style={s.brand}>REZFLOW</Text><Text style={s.hello}>Olá</Text><Text style={s.meta}>Igreja · Campus · MEMBRO</Text><Button label="SAIR" onPress={()=>void leave()} variant="secondary"/></View></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{flex:1,gap:spacing.lg,padding:spacing.lg},brand:{color:colors.primary,fontFamily:fontFamily.bold,fontSize:34},hello:{color:colors.white,fontFamily:fontFamily.bold,fontSize:36},meta:{color:colors.mutedText,fontSize:16}});
