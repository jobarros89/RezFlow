import { useState } from 'react'; import { Alert, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Button } from '@/components/ui/Button'; import { TextField } from '@/components/ui/TextField'; import { colors, fontFamily, spacing } from '@/constants/theme'; import { authService } from '@/services/auth.service';
export default function ForgotPassword(){const[email,setEmail]=useState('');const submit=async()=>{try{await authService.resetPassword(email);Alert.alert('E-mail enviado','Confira sua caixa de entrada.');}catch(e){Alert.alert('Não foi possível enviar',e instanceof Error?e.message:'Tente novamente.');}};return <SafeAreaView style={s.safe}><View style={s.content}><Text style={s.title}>RECUPERAR SENHA</Text><Text style={s.text}>Informe seu e-mail para receber as instruções.</Text><TextField icon="mail-outline" label="E-MAIL" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address"/><Button label="ENVIAR INSTRUÇÕES" onPress={()=>void submit()}/></View></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{gap:spacing.lg,padding:spacing.lg},title:{color:colors.white,fontFamily:fontFamily.bold,fontSize:34},text:{color:colors.mutedText,fontSize:16}});
