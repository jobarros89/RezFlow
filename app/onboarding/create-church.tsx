import { useState } from 'react'; import { Alert, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Button } from '@/components/ui/Button'; import { TextField } from '@/components/ui/TextField'; import { colors,fontFamily,spacing } from '@/constants/theme'; import { supabase } from '@/lib/supabase';
export default function CreateChurch(){const[n,setN]=useState(''),[c,setC]=useState(''),[city,setCity]=useState(''),[state,setState]=useState('');const submit=async()=>{const{error}=await supabase.rpc('create_church_with_membership',{p_church_name:n,p_campus_name:c,p_city:city,p_state:state});if(error)Alert.alert('Não foi possível criar',error.message);else Alert.alert('Igreja criada','Você recebeu a função DM.');};return <SafeAreaView style={s.safe}><View style={s.content}><Text style={s.title}>Crie sua igreja</Text><TextField icon="business-outline" label="NOME DA IGREJA" value={n} onChangeText={setN}/><TextField icon="location-outline" label="CAMPUS PRINCIPAL" value={c} onChangeText={setC}/><TextField icon="location-outline" label="CIDADE" value={city} onChangeText={setCity}/><TextField icon="map-outline" label="ESTADO" value={state} onChangeText={setState}/><Button label="CRIAR IGREJA" onPress={()=>void submit()}/></View></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{gap:spacing.md,padding:spacing.lg},title:{color:colors.white,fontFamily:fontFamily.bold,fontSize:34}});
