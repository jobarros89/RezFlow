import { useState } from 'react'; import { Alert, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Button } from '@/components/ui/Button'; import { TextField } from '@/components/ui/TextField'; import { colors,fontFamily,spacing } from '@/constants/theme';
export default function FindChurch(){const[q,setQ]=useState('');return <SafeAreaView style={s.safe}><View style={s.content}><Text style={s.title}>Encontre sua igreja</Text><TextField icon="search-outline" label="NOME DA IGREJA" placeholder="Busque pelo nome" value={q} onChangeText={setQ}/><Button label="BUSCAR IGREJAS" onPress={()=>Alert.alert('Em breve','A busca consultará as igrejas cadastradas no Supabase.')}/></View></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{gap:spacing.lg,padding:spacing.lg},title:{color:colors.white,fontFamily:fontFamily.bold,fontSize:34}});
